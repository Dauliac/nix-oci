schemaVersion: 2.0.0
metadataTest:
  envVars:
    - key: USER
      value: redis-default
fileExistenceTests:
  - name: "redis-server binary exists"
    path: "/bin/redis-server"
    shouldExist: true
  - name: "passwd file exists"
    path: "/etc/passwd"
    shouldExist: true
  - name: "SSL CA bundle exists"
    path: "/etc/ssl/certs/ca-bundle.crt"
    shouldExist: true
  - name: "home directory exists"
    path: "/home/redis-default"
    shouldExist: true
fileContentTests:
  - name: "non-root user in passwd"
    path: "/etc/passwd"
    expectedContents:
      - "redis-default:x:4000:4000"
